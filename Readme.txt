To run the application, install all the packages in the dependencies section specified in package.json.
once you have installed all the specified packages, run node app.js to start the application.

The application is a simple blog post app with functionalities like add,delete,edit a post.
preview running application url: https://nameless-sea-15125.herokuapp.com/blogs